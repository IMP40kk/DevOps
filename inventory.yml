allhosts:
  hosts:
    master:
      ansible_host: "{{ DB_HOST }}"
      ansible_user: ansible
      ansible_password: 1
    slave:
      ansible_host: "{{ DB_REPL_HOST }}"
      ansible_user: ansible
      ansible_password: 1
